@model IEnumerable<EDzienik.Entities.Teacher>

@{
    ViewData["Title"] = "Nauczyciele";
}

<div class="card shadow mb-4 border-left-primary">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Lista kadry pedagogicznej</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Imię i Nazwisko</th>
                        <th>Email</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @if (item.User != null)
                                {
                                    <span class="fw-bold text-dark">@item.User.FirstName @item.User.LastName</span>
                                }
                                else
                                {
                                    <span class="text-muted fst-italic">(Brak powiązanego konta)</span>
                                }
                            </td>
                            <td>
                                @if (item.User != null)
                                {
                                    @item.User.Email
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>